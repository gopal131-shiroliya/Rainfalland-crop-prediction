services:
  - type: web
    name: rainfall-crop-prediction
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn nawsp.api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
